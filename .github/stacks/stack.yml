name: stacks-starter
description: This is a stack to create a stack.
branding:
  icon: layers
  color: blue

inputs:
  - name: Is your stack gonna rock?
    default: true
    type: boolean
    description: Currently, there is a limitation that stacks need at least one input. It's getting removed soon. The response here does not really matter but I have a hunch it's true.

init:
 uses: setup.yml
